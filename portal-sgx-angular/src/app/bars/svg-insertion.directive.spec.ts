import { SvgInsertionDirective } from './svg-insertion.directive';

describe('SvgInsertionDirective', () => {
  it('should create an instance', () => {
    const directive = new SvgInsertionDirective();
    expect(directive).toBeTruthy();
  });
});
